[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "paper-m3glvq"
version = "0.1.0"
description = "ECML paper repository for structural analysis, single-view MGLVQ, and M3GLVQ"
requires-python = ">=3.10"